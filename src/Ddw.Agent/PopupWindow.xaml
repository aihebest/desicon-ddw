<Window x:Class="Ddw.Agent.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        AllowsTransparency="True" Background="Transparent" SizeToContent="WidthAndHeight">
    <Grid x:Name="RootGrid" Background="Transparent">
        <Border x:Name="Card" Width="380" CornerRadius="10" Background="White"
                BorderBrush="#E5E7EB" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.35" Color="#000000"/>
            </Border.Effect>
            <StackPanel>
                <Border x:Name="HeaderBar" Background="#1F4E79" CornerRadius="10,10,0,0" Padding="16,12">
                    <DockPanel>
                        <TextBlock Text="DESICON ENGINEERING" Foreground="#CFE0F2" FontWeight="Bold" FontSize="10"/>
                        <TextBlock x:Name="PriorityLabel" Text="ANNOUNCEMENT" Foreground="White" FontWeight="Bold"
                                   FontSize="13" HorizontalAlignment="Right"/>
                    </DockPanel>
                </Border>
                <StackPanel Margin="20">
                    <TextBlock x:Name="TitleText" FontSize="17" FontWeight="Bold" TextWrapping="Wrap" Foreground="#111827"/>
                    <TextBlock x:Name="BodyText" Margin="0,10,0,0" FontSize="13.5" TextWrapping="Wrap" Foreground="#374151"/>
                    <TextBlock x:Name="MetaText" Margin="0,14,0,0" FontSize="11" Foreground="#9CA3AF"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                        <Button x:Name="DismissBtn" Content="Dismiss" Width="90" Height="36" Margin="0,0,10,0"
                                Background="#F3F4F6" BorderThickness="0" Click="Dismiss_Click"/>
                        <Button x:Name="ActionBtn" Content="Acknowledge" Width="140" Height="36"
                                Background="#1F4E79" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Click="Action_Click"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
